let input = [5,6,9,8,7,6];

function sort(arr){
    const sortedArr = [];
    let nextIndex=0;
    for (let index in arr){
        if(arr[index]===(arr.length -1)){
            sortedArr[nextIndex]=arr[index]
        }
        if(arr[index]>arr[index+1]){

        }       
    } 
}